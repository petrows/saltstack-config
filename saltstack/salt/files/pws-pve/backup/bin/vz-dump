#!/bin/bash

# Backup only machines, having tag 'backup'. Exclude all others.
# This allows to exclude test machines, which are running on the same host.

pct-vzdump-all /srv/backup/vz /srv/backup/tmp
