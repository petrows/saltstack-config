#!/bin/bash

set -e

# Mount backup HDD
/opt/backup/bin/backup-mount

# Test external backup flag
if [[ -f /etc/backup-ext.lock ]]; then
    echo "External backup in progress, aborting"
    exit 1
fi

exit 0

